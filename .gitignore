# build output
dist/
# dependencies
node_modules/
# agent scratch + linked git worktrees live here — never commit
.claude/
# generated types
.astro/
# environment (secrets / per-deploy config)
.env
.env.production
# logs
*.log
npm-debug.log*
# macOS
.DS_Store
